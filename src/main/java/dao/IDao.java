// Déclaration du package "dao"
package dao;

// Définition de l'interface IDao
public interface IDao {
    // Déclaration de la méthode getData, qui ne contient que la signature (pas de corps de méthode)
    double getData();

}

